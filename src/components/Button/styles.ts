import styled from 'styled-components';

export const Container = styled.button`
  border: none;
  border-radius: 5px;
  padding: 10px;
  color: #fff;

  &:hover {
    opacity: 0.9;
    cursor: pointer;
  }
`;
